# ==============================================================================
# 🔐 ENVIRONMENT VARIABLES (EXAMPLE)
# ==============================================================================
# Instructions:
# 1. Copy this file and rename it to ".env.local"
# 2. Fill in your actual secrets.
# 3. DO NOT commit your real ".env.local" file to GitHub!
# ==============================================================================

# ------------------------------------------------------------------------------
# 🗄️ DATABASE (PRISMA + MONGODB)
# ------------------------------------------------------------------------------
# Replace with your actual MongoDB connection string (Atlas or Local)
# Format: mongodb+srv://<username>:<password>@<cluster>.mongodb.net/<db_name>
DATABASE_URL="mongodb+srv://username:password@cluster.mongodb.net/my-database?retryWrites=true&w=majority"

# ------------------------------------------------------------------------------
# 🛡️ BETTER AUTH CONFIGURATION (we use in this project better-auth if you need another setup it manually)
# ------------------------------------------------------------------------------
# You can generate a new secret by running: openssl rand -hex 32
# Or just type a very long random string here.
#you can you for this auth npm package jr-auth-cli (how to add it just terminnal command jr-auth-cli add )
BETTER_AUTH_SECRET="replace_this_with_a_long_random_secure_string"

# The base URL of your application
# For Development: http://localhost:3000
# For Production: https://your-domain.com
BETTER_AUTH_URL="http://localhost:3000"

# ------------------------------------------------------------------------------
# 🌐 SOCIAL PROVIDERS (OPTIONAL)
# ------------------------------------------------------------------------------
# Google OAuth (https://console.cloud.google.com/)
GOOGLE_CLIENT_ID=""
GOOGLE_CLIENT_SECRET=""

# GitHub OAuth (https://github.com/settings/developers)
GITHUB_CLIENT_ID=""
GITHUB_CLIENT_SECRET=""

# ------------------------------------------------------------------------------
# ⚙️ PUBLIC VARIABLES
# ------------------------------------------------------------------------------
# Useful for absolute URLs in frontend
NEXT_PUBLIC_APP_URL="http://localhost:3000"